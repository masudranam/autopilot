# The React 19 / Next 16 ecosystem still ships packages with stale peer ranges.
# Warn rather than hard-fail installs on them.
strict-peer-dependencies=false

# Keep the lockfile authoritative in CI.
prefer-frozen-lockfile=true
